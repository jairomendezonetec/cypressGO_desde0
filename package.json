{
	"name": "cypress_desde0",
	"version": "1.0.0",
	"description": "Pruebas automatizadas con cypress desde cero",
	"main": "index.js",
	"scripts": {
		"open": "cypress open",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"cypress": "^9.6.0",
		"cypress-cucumber-preprocessor": "^4.3.1",
		"cypress-xpath": "^1.6.2"
	},
	"cypress-cucumber-preprocessor": {
		"nonGlobalStepDefinitions": true,
		"cucumberJson": {
			"generate": true,
			"outputFolder": "cypress/cucumber-json",
			"filePrefix": "",
			"fileSuffix": ".cucumber"
		}
	}
}
